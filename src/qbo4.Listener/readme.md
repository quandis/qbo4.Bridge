# qbo4.Listener

This offers a `HttpListener` enabling qbo life cycle events to be propagated by `http` or `https` notifications.

See also:

- Amazon [SQS notifications to private instances](https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-vpc-endpoints.html).